// File intentionally blanked during migration.
// The legacy per-tab provider was removed — use `GlobalDndProvider` mounted at app root instead.
// This file remains only to avoid accidental package-resolution errors while branches are migrated.

export default undefined;
